"""
Up and down
Define a function named up_down that takes a single number as its 
parameter. Your function return a tuple containing two numbers; 
the first should be one lower than the parameter, and the second 
should be one higher.

For example, calling up_down(5) should return (4, 6).
"""
"""
up_down adında bir fonksiyon tanımlayınız. Tek bir sayıyı parametre olarak alsın.
Fonksiyonunuz iki sayılı bir tuple dönsün. 
İlk sayı parametre olarak verilen sayının bir düşüğü, 
ikinci sayı ise bir yükseği olsun.

Mesela up_down(5) fonksiyonunu çağırdığımızda (4,6) dönmeli.
"""
