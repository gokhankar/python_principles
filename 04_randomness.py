"https://pythonprinciples.com/challenges/Randomness/"


"""
Randomness
Define a function, random_number, that takes no parameters. 
The function must generate a random integer between 1 and 100, 
both inclusive, and return it.

Calling the function multiple times should (usually) return different numbers.

For example, calling random_number() some times might first return 42, then 63, then 1.
"""
"""
random_number adında bir fonksiyon yazınız. Bu fonksiyon parametre almayacaktır.
Bu fonksiyon 1 ve 100 dahil, bir integer tamsayı yaratacak, ve bu sayıyı dönecektir.
Eğer fonksiyon birkaç kez çağırılırsa, (çoğunlukla) farklı numaralar dönmelidir.
Mesela, random_number() fonksiyonu ardarda çağrıldığında mesela 42 döner, bir sonrakine
63 döner, bir sonrakine 1 döner. 
"""
